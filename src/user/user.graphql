scalar Date

type User {
  _id: String!
  username: String!
  created_at: Date!
  updated_at: Date!
}

type Query {
  user(id: String!): User!
  users: [User!]
}

input UserInput {
  username: String!
  password: String!
}

type Mutation {
  createUser(input: UserInput!): User
}
